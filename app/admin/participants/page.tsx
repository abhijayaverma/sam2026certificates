import { requireAdmin } from '@/lib/auth'; import { adminClient } from '@/lib/supabase'; import { AdminShell } from '@/components/AdminShell';
export default async function Participants(){await requireAdmin(); const {data}=await adminClient().from('participants').select('*').order('created_at',{ascending:false}).limit(100); return <AdminShell><h1 className="text-4xl font-black">Participants</h1><div className="mt-6 overflow-hidden rounded-3xl bg-white shadow"><table className="w-full text-left"><thead className="bg-slate-50"><tr><th className="p-4">Roll Number</th><th className="p-4">Name</th><th className="p-4">Created</th></tr></thead><tbody>{data?.map(p=><tr key={p.id} className="border-t"><td className="p-4">{p.roll_number}</td><td className="p-4 font-semibold">{p.name}</td><td className="p-4">{new Date(p.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></AdminShell>}
